export enum ContributionStatusEnum {
  Pending = "pending",
  Submitted = "submitted",
  Approved = "approved",
  Rejected = "rejected",
  Failed = "failed",
}

export enum ContributionKindEnum {
  NewRiver = "new-river",
  ExistingRiverPoint = "existing-river-point",
  RemoveExistingPoint = "remove-existing-point",
}